<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ renderData.title or title or metadata.title }}{% if renderData.title or title %} | {{ metadata.title}}{% endif %}</title>
  <style>
  </style>
</head>
<body>

<h1>CSS Gridを使ったレイアウト例</h1>

{{ content | safe }}

</body>
</html>